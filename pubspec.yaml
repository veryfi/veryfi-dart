name: veryfi_dart
description: OCR API for Real-Time Data Extraction. This package contains a set of functions that make it easy to consume and communicate with the Veryfi OCR API.
version: 0.0.4
homepage: https://www.veryfi.com

environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  http: ^0.13.4
  crypto: ^3.0.1

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  build_runner: ^2.1.8
  mockito: ^5.1.0
